java files in this folder is moved from test/runtime_client/src/org/.../test.
test cases in this folder have dependency on the build-in extensions which could be conditionally disabled. 
so the java files in this folder won't be compiled if the build-in extensions are disabled.